#include <stdio.h>
#include <conio.h>
void main()
{
	int i, s, n;
	printf("enter the number: ");
	scanf("%d", &n);
	for (i = 0; i <= n; i++)
	{

		s = i * i;
	}
	printf(" square of the number:%d", s);

	getch();
}
